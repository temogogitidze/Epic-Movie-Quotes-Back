components:
  responses:
    update-user:
      "200":
        description: OK
        content:
          application/json:
            schema:
              type: string
              example: success
